#include <algorithm>
#include <bits/stdc++.h>
#include <iostream>

#define MAIN() int main(){ ios::sync_with_stdio(false); cin.tie(0); cout.precision(10); cout << fixed; CASES() { Solution s; } }

// Data Structures and data types
#define ll long long
#define vll std::vector<ll>
#define vvll std::vector<vll>
#define mll std::map<ll, ll>
#define mcl std::map<char, ll>
#define umll std::unordered_map<ll, ll>
#define umcl std::unordered_map<char, ll>
#define pll std::pair<ll, ll>
#define dbl double;

// Loops
#define CASES() ll t; cin>>t; while(t--)
#define FOR(i,n) for(ll i = 0; i<n; i++)
#define FOR_1(i,n) for(ll i = 1; i<=n; i++)
#define FOR_REV(i,n) for(ll i=n-1; n>=0; --i)
#define FOR_REV_1(i,n) for(ll i=n; n>=1; --i)

// Common tokens in stl
#define pb push_back
#define fi first
#define se second

// Traversal / Iterators
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()

// Check if exists
#define exists(c,e) (find(all(c),x) != (c.end()))

// Get size while avoiding wrap around due to unsignedj
#define sz(a) (ll)((a).size())


using namespace std;

class Solution {
public:
    Solution(){
        // Solution Code
    }
};

MAIN()
